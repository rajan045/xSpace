# Marketing site pricing page (opened from app paywall “Buy license”).
VITE_PRICING_URL=http://localhost:3000/pricing

# xspace-api base URL (no trailing slash) — auth, token validate, trial sync, pricing.
# Optional: if unset, the app uses http://localhost:4000. Set this for staging/production APIs.
XSPACE_API_URL=https://dev-x-space-backend.dhql6p.easypanel.host/
