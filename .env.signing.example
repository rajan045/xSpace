# Copy to .env.signing (gitignored), then run `npm run build:electron:release` (fails if vars missing).
# Or `npm run build:electron` once this file is filled (same notarization hook).
# App-specific password: https://appleid.apple.com/account/manage → App-Specific Passwords
APPLE_ID=kavin@bytive.in
APPLE_APP_SPECIFIC_PASSWORD=lqxe-gstg-cowu-ojca
APPLE_TEAM_ID=MYUW23F22J
